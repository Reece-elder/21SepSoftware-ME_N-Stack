`npm init -y` Use default names for EVERYTHING 

With npm scripts you have to specify `npm run <script name>`

`npm restart`
`npm start`
`npm stop`
`npm test`

To remove dependencies / packages use
`npm remove <package name>`